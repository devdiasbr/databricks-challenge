# Core Dependencies (Databricks already provides pyspark/delta-spark)
python-dotenv==1.0.0
tqdm==4.66.1

# Optional/Local Development (Uncomment if running locally)
pyspark==3.5.0
delta-spark==3.0.0

# Azure SDK (Use version compatible with Databricks Runtime)
azure-storage-blob>=12.0.0
